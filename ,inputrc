# Alex Hahn's .inputrc for ksh shell
# Set command line editing to be like vi(m)
set editing-mode vi
# Set Autocomplete to ignore case
set completion-ignore-case on
# Make bash 8bit clean
set meta-flag on
set convert-meta off
set output-meta on
